export default function primeiro() {
    return "Primeiro"
}

// uma vez criada uma função dentro de um arquivo (que em Node JS arquivo representa um módulo)
// para que essa função fique visível para além deste arquivo, preciso exportá-la por padrão,
// usando o comando 'export default'